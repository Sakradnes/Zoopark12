const React = require('react')
const Layout = require('./Layout')

function getCard (){
    return (
        <Layout>
            <div>
                
            </div>
        </Layout>
    )
}

module.exports = getCard